name: Docs sync

on:
  pull_request:
    types: [opened, synchronize, reopened]
    # Event-level guard: when every changed file is documentation, no job is created.
    paths-ignore:
      - 'docs/**'
      - '*.md'

permissions:
  contents: read

concurrency:
  group: docs-sync-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  plan:
    # Internal branches only. claude/* is reserved for generated commits.
    if: >-
      github.event.pull_request.head.repo.fork == false &&
      !startsWith(github.event.pull_request.head.ref, 'claude/')
    runs-on: ubuntu-latest
    # Per-doc reasoning is sequential, so the ceiling scales with how many
    # covers: globs a branch hits. 15 minutes died at 11 matched docs.
    timeout-minutes: 40
    permissions:
      contents: read
    outputs:
      publish: ${{ steps.map.outputs.publish }}
    steps:
      - name: Check out base SHA (trusted scripts only)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: trusted
          persist-credentials: false
          fetch-depth: 0
      - name: Fetch PR head as data
        working-directory: trusted
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: git fetch --no-tags origin "$HEAD_SHA"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: trusted/.nvmrc
      - name: Map changed files to docs
        id: map
        working-directory: trusted
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if git log -1 --format=%s "$HEAD_SHA" | grep -qE '^docs: sync for #[0-9]+'; then
            echo "publish=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git diff --name-status -z --find-renames "$BASE_SHA...$HEAD_SHA" > "$RUNNER_TEMP/name-status.z"
          node --input-type=module - "$RUNNER_TEMP/name-status.z" "$RUNNER_TEMP/changed.txt" <<'NODE'
          import { readFileSync, writeFileSync } from 'node:fs';
          import { parseNameStatusZ } from './scripts/docs-sync/diff.mjs';
          writeFileSync(process.argv[3], `${parseNameStatusZ(readFileSync(process.argv[2])).join('\n')}\n`);
          NODE
          if ! grep -qvE '^(docs/|[^/]+\.md$)' "$RUNNER_TEMP/changed.txt"; then
            echo "publish=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Jumbo-PR guard: per-doc planning assumes a reviewable diff. Beyond
          # 50 changed code files the agent blows the 15-minute job budget, so
          # skip the sync and leave documentation to the PR author (reference
          # docs are still enforced by the drift check in pnpm test).
          code_changed=$(grep -cvE '^(docs/|[^/]+\.md$)' "$RUNNER_TEMP/changed.txt" || true)
          if [ "$code_changed" -gt 50 ]; then
            echo "::notice::docs-sync skipped: $code_changed changed code files exceeds the 50-file planning budget. Sync docs in this PR by hand or run docs-sync on a smaller follow-up."
            echo "publish=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          node scripts/docs-map.mjs < "$RUNNER_TEMP/changed.txt" > "$RUNNER_TEMP/map.json"
          node --input-type=module - "$RUNNER_TEMP/map.json" <<'NODE'
          import { readFileSync } from 'node:fs';
          const map = JSON.parse(readFileSync(process.argv[2], 'utf8'));
          const escape = (value) => String(value).replaceAll('%', '%25').replaceAll('\r', '%0D').replaceAll('\n', '%0A');
          for (const entry of map.manualReview ?? []) {
            const path = typeof entry === 'string' ? entry : entry.path;
            const reason = typeof entry === 'string' ? 'published manual-policy documentation may need review' : entry.reason;
            console.log(`::warning file=${escape(path)}::${escape(reason ?? 'published manual-policy documentation may need review')}`);
          }
          NODE
          matched=$(node -e 'const m=require(process.argv[1]); process.stdout.write(String(m.matched.length))' "$RUNNER_TEMP/map.json")
          if [ "$matched" -eq 0 ]; then echo "publish=0" >> "$GITHUB_OUTPUT"; else echo "publish=1" >> "$GITHUB_OUTPUT"; fi
      - name: Claude authentication smoke
        if: steps.map.outputs.publish == '1'
        working-directory: trusted
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        # Verified against 2.1.212 --help: --tools limits available built-ins;
        # --safe-mode disables settings, CLAUDE.md, hooks, plugins, skills, and MCP.
        run: >-
          npx --yes @anthropic-ai/claude-code@2.1.212 --model claude-sonnet-4-5-20250929
          --print --safe-mode
          --disable-slash-commands --strict-mcp-config --mcp-config '{"mcpServers":{}}'
          --tools '' --allowed-tools '' --no-session-persistence
          'Reply with exactly: ok' | grep -qix ok
      - name: Isolated per-doc reasoning
        if: steps.map.outputs.publish == '1'
        working-directory: trusted
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        # plan.mjs passes one doc + diff over stdin with all Claude tools disabled,
        # then validates schema output before deterministically staging the doc.
        run: node scripts/docs-sync/plan.mjs "$RUNNER_TEMP/map.json" "$RUNNER_TEMP/docs-sync-staging" "$PWD"
      - name: Upload edited docs and allowlist
        if: steps.map.outputs.publish == '1'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: docs-sync-staging
          path: ${{ runner.temp }}/docs-sync-staging
          if-no-files-found: error
          retention-days: 1

  validate:
    needs: plan
    if: needs.plan.result == 'success' && needs.plan.outputs.publish == '1'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Check out base SHA (trusted validation code)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: trusted
          persist-credentials: false
      - name: Download planned edits
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: docs-sync-staging
          path: ${{ runner.temp }}/docs-sync-staging
      - name: Check out pinned PR head without credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.6.4
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: trusted/.nvmrc
          # No pnpm cache here: this job installs from the untrusted PR head, and
          # setup-node keys its cache by lockfile hash alone. If the PR leaves the
          # lockfile unchanged, that key collides with the credentialed publish
          # job's, letting an untrusted install warm a cache publish would restore.
          # Install fresh instead so the write-privileged job shares no cache with it.
      - name: Install PR-head validation dependencies
        working-directory: pr
        run: >-
          pnpm install --frozen-lockfile
          --filter @opslane/docs-site...
          --filter opslane-fixture-react...
          --filter opslane-fixture-app...
      - name: Validate proposed docs against PR head
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: >-
          node trusted/scripts/docs-sync/validation-job.mjs
          "$RUNNER_TEMP/docs-sync-staging"
          "$RUNNER_TEMP/docs-sync-staging/map.json"
          "$GITHUB_WORKSPACE/pr"

  publish:
    needs: [plan, validate]
    if: >-
      needs.plan.result == 'success' &&
      needs.validate.result == 'success' &&
      needs.plan.outputs.publish == '1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Check out base SHA (trusted scripts only)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: trusted
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.6.4
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: trusted/.nvmrc
          cache: pnpm
          cache-dependency-path: trusted/pnpm-lock.yaml
      - name: Install trusted validation dependencies
        working-directory: trusted
        run: pnpm install --frozen-lockfile --filter @opslane/docs-site...
      - name: Download planned edits
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: docs-sync-staging
          path: ${{ runner.temp }}/docs-sync-staging
      - name: Check out pinned PR head for publication
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr
          fetch-depth: 0
          # PR files remain data: trusted publish code overlays them beneath
          # the trusted base's docs-site implementation for validation.
      - name: Validate, scan, commit, and guarded push
        env:
          PR: ${{ github.event.pull_request.number }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: >-
          node trusted/scripts/docs-sync/publish.mjs
          "$RUNNER_TEMP/docs-sync-staging"
          "$RUNNER_TEMP/docs-sync-staging/map.json"
          "$GITHUB_WORKSPACE/pr"
