{"base_sha": "712e02373f1c5ac72fdf5da16f04838338ba817e", "fix_sha": "a75bf44647e5132e39b778730394b0a2b983265c", "ground_truth": ["lib/core/Axios.js"], "issue": 11108, "issue_body": "### Describe the bug\n\nWhen the global `Error.prepareStackTrace` is overridden to return a non-string value \u2014 a common instrumentation pattern (APM agents, `callsites`-style libraries return raw CallSite data) \u2014 the stack-decoration code in `Axios.prototype.request`'s catch block throws its own `TypeError`, and **the original request error is lost**.\n\nThe guard only checks truthiness:\n\n```js\n// lib/core/Axios.js\nconst stack = (() => {\n  if (!dummy.stack) {   // truthy non-strings pass this check\n    return '';\n  }\n  const firstNewlineIndex = dummy.stack.indexOf('\\n');  // TypeError if stack is not a string/array\n  ...\n```\n\nSo instead of the real failure (e.g. `ECONNREFUSED`, a TLS error, a 5xx from an OAuth token endpoint), callers receive `TypeError: dummy.stack.indexOf is not a function` (on axios <= 1.12: `dummy.stack.replace is not a function`). Since this happens in the error path, the cosmetic stack decoration destroys the error it was decorating. The very next block in the same function already wraps its `err.stack` manipulation in a `try/catch` for hostile environments \u2014 this line just lacks the same defense.\n\nReal-world impact example: in n8n, failed OAuth2 token refreshes surfaced as this opaque TypeError instead of the actual auth error, making credential failures undiagnosable (n8n-io/n8n#26453).\n\n### To Reproduce\n\n```js\nError.prepareStackTrace = () => ({}); // any truthy non-string, e.g. structured call-site data\nconst axios = require('axios');\naxios.get('http://127.0.0.1:1').catch(e => console.log(e.message));\n// actual:   \"dummy.stack.indexOf is not a function\"\n// expected: \"connect ECONNREFUSED 127.0.0.1:1\"\n```\n\n### Expected behavior\n\nThe original error propagates unchanged; a non-string `dummy.stack` merely skips the stack decoration.\n\nFix is a one-liner \u2014 `if (!dummy.stack)` \u2192 `if (typeof dummy.stack !== 'string')`. PR with a regression test to follow.\n\n### Axios Version\n\n1.18.1 (also present in every version since the decoration was introduced)\n\n### Node.js Version\n\n24.x (any)\n\n### OS\n\nany\n", "issue_title": "Request error is destroyed by stack decoration when Error.prepareStackTrace returns a non-string", "pr": 11109, "pr_title": "fix(core): tolerate non-string stack when decorating request errors", "repo": "axios/axios"}
{"base_sha": "529ce70296334a0397f9e45e7dfa5658f6b6fdba", "fix_sha": "c3f553c740ebf3dff5e22dae24e9caaafafddd2d", "ground_truth": ["lib/adapters/xhr.js", "lib/core/buildFullPath.js", "lib/helpers/normalizeURLForProtocolCheck.js"], "issue": 11093, "issue_body": "## Describe the issue\n\naxios contradicts itself about what `status === 0` means, and Firefox 152 made the disagreement reachable.\n\nThe XHR adapter has the rule right, and scopes the `file:` exception properly:\n\n```js\n// lib/adapters/xhr.js, the onreadystatechange fallback\n// The request errored out and we didn't get a response, this will be\n// handled by onerror instead\n// With one exception: request that using file: protocol, most browsers\n// will return status as 0 even though it's a successful request\nif (request.status === 0 && !(request.responseURL && request.responseURL.startsWith('file:'))) {\n  return;\n}\n```\n\n`settle()` does not scope it at all. It resolves any status-0 response as a success, on any scheme:\n\n```js\n// lib/core/settle.js\nif (!response.status || !validateStatus || validateStatus(response.status)) {\n  resolve(response);\n}\n```\n\nThe catch is where that adapter guard lives. It sits in the `onreadystatechange` fallback, which only runs when `'onloadend' in request` is false, and that is never the case in a current browser. Every evergreen browser takes the `onloadend` path instead, which has no equivalent check, so the unscoped version in `settle()` is the one that actually runs.\n\nFor about six years this did not matter, because browsers always fired `error` or `abort` before `loadend` on a failed or canceled XHR. `onabort`/`onerror` would reject and set `request = null`, and `onloadend` would bail on `if (!request) return` before `settle()` ever ran.\n\nFirefox 152 changed that. [Bugzilla 1505389](https://bugzilla.mozilla.org/show_bug.cgi?id=1505389) made navigation-canceled XHRs fire only `readystatechange` and `loadend`, with no `abort` and no `error`. The patch says it outright:\n\n> Don't make navigation block XHR and call onerror/abort\n>\n> Fires readystatechange and loadend intead of abort\n\nIt is intentional and it has been upstreamed. Mozilla landed a conformance test in web-platform-tests ([wpt#59603](https://github.com/web-platform-tests/wpt/pull/59603), `xhr/abort-during-navigation.window.js`) asserting that `error`, `abort` and `load` must not fire. Chrome already fires nothing and passes it. Safari still fires `abort` and would fail it. So this looks like where the platform is heading rather than a Firefox quirk that will get reverted.\n\nNote also that `!response.status` is evaluated before `validateStatus`, so this cannot be worked around with config.\n\n## Example code\n\nAny request that is in flight when the user navigates away, on Firefox 152 or later:\n\n```js\n// Page navigates (link click, location assignment, reload) while this is pending.\nconst { data } = await axios.get('/api/things');\n// Firefox <= 151: rejects with ECONNABORTED, because the adapter saw an abort event.\n// Firefox >= 152: RESOLVES, with status 0 and data === ''.\n// Anything parsing or validating `data` then fails on an empty string.\n```\n\nReproducible without a browser, by driving the real xhr adapter with a stub `XMLHttpRequest`. The only difference between the two runs is whether `error` fires:\n\n```js\nlet SEQUENCE = [];\nglobalThis.XMLHttpRequest = class {\n  constructor() {\n    this.readyState = 0; this.status = 0; this.statusText = '';\n    this.responseText = ''; this.response = ''; this.timeout = 0;\n    this.onloadend = this.onabort = this.onerror = this.ontimeout = this.onreadystatechange = null;\n  }\n  open() {} setRequestHeader() {} abort() {} getAllResponseHeaders() { return ''; }\n  send() {\n    setImmediate(() => {\n      this.readyState = 4;\n      for (const ev of SEQUENCE) this[`on${ev}`]?.call(this, { type: ev });\n    });\n  }\n};\nconst { default: axios } = await import('axios');\n\nfor (const [label, seq] of [\n  ['error precedes loadend (every browser before FF152)', ['readystatechange', 'error', 'loadend']],\n  ['loadend only        (Firefox >= 152 nav-cancel)     ', ['readystatechange', 'loadend']],\n]) {\n  SEQUENCE = seq;\n  const out = await axios.get('https://example.com/x', { adapter: 'xhr' })\n    .then((r) => `RESOLVED status=${r.status} data=${JSON.stringify(r.data)}`)\n    .catch((e) => `rejected  ${e.code}`);\n  console.log(label, '->', out);\n}\n```\n\nOutput on axios 1.18.1:\n\n```\nerror precedes loadend (every browser before FF152) -> rejected  ERR_NETWORK\nloadend only        (Firefox >= 152 nav-cancel)     -> RESOLVED status=0 data=\"\"\n```\n\nConfirmed in a real Firefox 153.0 as well, using the same iframe technique as Mozilla's WPT test. An in-flight XHR is canceled by a document navigation and the surviving parent records the events plus axios's outcome:\n\n```json\n{\"ua\":\"Mozilla/5.0 (Macintosh; Intel Mac OS X 10.15; rv:153.0) Gecko/20100101 Firefox/153.0\",\n \"events\":[\"loadend(st=0)\"],\n \"firedAbort\":false, \"firedError\":false, \"firedLoad\":false,\n \"axios\":{\"outcome\":\"RESOLVED\",\"status\":0,\"dataType\":\"string\",\"data\":\"\\\"\\\"\"}}\n```\n\n## Expected behavior\n\nA request that never received a response should reject, as it did on Firefox 151 and as it still does in every other engine. Reads over `file:`, which some environments report as status 0 on success, should keep resolving.\n\n## Actual behavior\n\nIt resolves as a success carrying `status: 0` and `data: ''`. No error is thrown, so the failure surfaces later as a parsing or validation error on an empty string, far from its cause.\n\n## Environment\n\n- Axios version: 1.18.1 (behavior present since 0.19.1, when #2470 landed)\n- Adapter: xhr\n- Runtime: Firefox 152 and later, verified on Firefox Developer Edition 153.0. Chrome and Safari are unaffected today, though Safari changes if it adopts wpt#59603\n- OS: macOS 15\n- Additional context: browser build, default adapter, no custom `validateStatus`\n\n:surfer: Investigated and written with AI assistance, reviewed before posting.\n", "issue_title": "XHR adapter resolves navigation-canceled requests as success on Firefox 152+ (status 0, empty body)", "pr": 11094, "pr_title": "fix(xhr): reject navigation-canceled requests as ECONNABORTED", "repo": "axios/axios"}
{"base_sha": "609dacd8cce089adc7720e11cd62405adf3b6867", "fix_sha": "712e02373f1c5ac72fdf5da16f04838338ba817e", "ground_truth": ["lib/core/InterceptorManager.js"], "issue": 11070, "issue_body": "### Describe the bug\n\n`InterceptorManager` stores interceptors in a plain array (`this.handlers`). When `eject(id)` is called, it sets the slot to `null` rather than removing it:\n\nIn applications that frequently add and remove interceptors (e.g., per-request auth interceptors, retry logic), this causes unbounded growth of the `handlers` array with `null` entries. Every request dispatch must filter these out.\n\n### Steps to reproduce\n\nconst instance = axios.create();\n\nfor (let i = 0; i < 10_000; i++) {\n  const id = instance.interceptors.request.use((cfg) => cfg);\n  instance.interceptors.request.eject(id);\n}\n\n// handlers array now has 10,000 null slots\nconsole.log(instance.interceptors.request.handlers.length); // 10000\n\n### Expected behavior\n\nThe `handlers` array should not grow unboundedly. Ejected interceptors should not leave behind `null` tombstones that accumulate over the lifetime of an axios instance.\n\n### Suggested fix\n\nReplace the Array with a Map keyed by an incrementing integer ID:\n\nconstructor() {\n  this.handlers = new Map();\n  this._idCounter = 0;\n}\n\nuse(fulfilled, rejected, options) {\n  const id = this._idCounter++;\n  this.handlers.set(id, {\n    fulfilled,\n    rejected,\n    synchronous: options?.synchronous ?? false,\n    runWhen: options?.runWhen ?? null,\n  });\n  return id;\n}\n\neject(id) {\n  this.handlers.delete(id);\n}\n\nThis is a non-breaking change \u2014 the returned ID is still a number and eject(id) still works the same way from the caller's perspective.\n\n### Environment\n\n- axios version: v1.x (latest)\n- Affected file: lib/core/InterceptorManager.js", "issue_title": "InterceptorManager sparse array growth", "pr": 11087, "pr_title": "fix(interceptors): trim trailing ejected handlers", "repo": "axios/axios"}
{"base_sha": "ff60b43277c32a5b2f7589c917db16d8e043c0d4", "fix_sha": "a93b1c90d919542987cc38591d10eed949a345d0", "ground_truth": ["lib/core/AxiosError.js"], "issue": 6721, "issue_body": "### Describe the bug\n\nWe have an intermittent issue on a service making external HTTPS API calls.\r\nThe actual issue isn't particularly relevant, but our problem is that Axios swallowed most of the underlying AggregateError making diagnosis hard.\r\n\r\nNode was throwing an AggregateError combining a number of ETIMEDOUT and EHOSTUNREACH errors, contained in a `.errors` collection - but the parent AggregateError object had a code `ETIMEDOUT` (Node just uses the code of the first nested error), a _blank_ message, and a name `AggregateError`\r\n\r\nWe were logging just the message and the code (as our payloads have sensitive data so we avoided logging the whole Error object - it contains the payload!) and this made it quite difficult to work out that this was more than just a time out.\r\n\r\nEventually I managed to get a stack trace which made things clearer:\r\n\r\n```text\r\nAggregateError\r\n    at AxiosError.from (file:///workspace/node_modules/axios/lib/core/AxiosError.js:92:14)\r\n    at RedirectableRequest.handleRequestError (file:///workspace/node_modules/axios/lib/adapters/http.js:620:25)\r\n    at RedirectableRequest.emit (node:events:530:35)\r\n    at RedirectableRequest.emit (node:domain:489:12)\r\n    at eventHandlers.<computed> (/workspace/node_modules/follow-redirects/index.js:49:24)\r\n    at ClientRequest.emit (node:events:518:28)\r\n    at ClientRequest.emit (node:domain:489:12)\r\n    at emitErrorEvent (node:_http_client:101:11)\r\n    at TLSSocket.socketErrorListener (node:_http_client:504:5)\r\n    at TLSSocket.emit (node:events:518:28)\r\n    at Axios.request (file:///workspace/node_modules/axios/lib/core/Axios.js:45:41)\r\n    at process.processTicksAndRejections (node:internal/process/task_queues:95:5)\r\n    at async requestAuthToken (file:///workspace/dist/src/auth-endpoint-handler.js:58:12)\r\n    at async file:///workspace/dist/src/auth-endpoint-handler.js:25:27\r\n```\r\n\r\n(if it helps we are using Axios 1.7.7 on NodeJS 20.18)\n\n### To Reproduce\n\n_No response_\n\n### Code snippet\n\n_No response_\n\n### Expected behavior\n\nIt'd be good to have an `Error.message` indicating that the problem is an AggregateError - or more generally, if wrapping an Error that has no message, setting a message to the name of the underlying error (or some other informative message)\n\n### Axios Version\n\n1.7.7\n\n### Adapter Version\n\n_No response_\n\n### Browser\n\n_No response_\n\n### Browser Version\n\n_No response_\n\n### Node.js Version\n\n20.18.1\n\n### OS\n\n_No response_\n\n### Additional Library Versions\n\n_No response_\n\n### Additional context/Screenshots\n\n```bash\nRunning via a Google Cloud Run Function on GCP\n```\n", "issue_title": "Node AggregateError is being returned as ETIMEDOUT with no message", "pr": 11059, "pr_title": "fix(core): synthesize AxiosError message from AggregateError.errors", "repo": "axios/axios"}
{"base_sha": "3d253b4f17a5107e6f274ee5b2e96e03508dceb2", "fix_sha": "97131238cab2e56f58f26b5bfd95e40bb5b9df14", "ground_truth": ["lib/adapters/http.js", "lib/helpers/progressEventReducer.js"], "issue": 6878, "issue_body": "### Describe the bug\n\nenv: node v22\nStarting from axios 1.7.3 (https://github.com/axios/axios/pull/6518), in node environment `onDownloadProgress` maybe invoke after request solved\nIt may be affected by the throttling method. Although the last task is finally executed through flush function, the execution order of queueMicroTask is later than response.data.on('close', callback)\n\n### To Reproduce\n\n```javascript\nconsole.log(\"start\");\n  const url =\n    \"https://fetch-progress.anthum.com/30kbps/images/sunrise-baseline.jpg\"; \n\n  const result = await axios.head(url, { headers: undefined });\n  const totalSize = result.headers[\"content-length\"];\n\n  axios\n    .get(url, {\n      headers: {\n        Range: `bytes=0-${totalSize}`,\n      },\n      responseType: \"stream\",\n      onDownloadProgress: (progressEvent) => {\n        console.log(\"progressEvent\", progressEvent);\n        const percentage = Math.round(\n          (progressEvent.loaded * 100) / progressEvent.total\n        );\n      },\n    })\n    .then((response) => {\n      response.data.on(\"close\", () => {\n        console.log(\"done\");\n      });\n    })\n    .catch((error) => {\n      console.log(error);\n    });\n```\n\noutput\n```Shell\n// ...\ndone\nprogressEvent xxx\n```\n\nIf the frequency of receiving file stream chunks is bigger than 3, this phenomenon will be triggered\n\n### Code snippet\n\n```js\n\n```\n\n### Expected behavior\n\n_No response_\n\n### Axios Version\n\n1.7.3\n\n### Adapter Version\n\n_No response_\n\n### Browser\n\n_No response_\n\n### Browser Version\n\n_No response_\n\n### Node.js Version\n\n20\n\n### OS\n\n_No response_\n\n### Additional Library Versions\n\n```bash\n\n```\n\n### Additional context/Screenshots\n\n```bash\n\n```", "issue_title": "onDownloadProgress occurred after request solved", "pr": 11040, "pr_title": "fix(http): flush download progress before stream close", "repo": "axios/axios"}
{"base_sha": "721ce4f6bd3c1f9400d45231b16a059531a16f6c", "fix_sha": "0aca1e0d84efaffacac03cabc00af92730985d2f", "ground_truth": ["packages/runtime-vapor/src/directives/vShow.ts"], "issue": 15202, "issue_body": "### Vue version\n\n3.6.0-rc.2\n\n### Link to minimal reproduction\n\n[playground](https://play.vuejs.org/#)\n\n### Steps to reproduce\n\nClick `update visible` once.\n\n\n### What is expected?\n\n`before-enter` should run exactly once and `count` should become `1`.\n\n### What is actually happening?\n\n`handleBeforeEnter` infinite recursion.\n\n\n### System Info\n\n```shell\n\n```\n\n### Any additional comments?\n\n_No response_", "issue_title": "[Vapor] mutating reactive state in a v-show transition hook causes recursive updates", "pr": 15203, "pr_title": "fix(runtime-vapor): avoid tracking v-show transition hooks", "repo": "vuejs/core"}
{"base_sha": "dff8dd5badaddea39f91da674be84d6af6f0f857", "fix_sha": "721ce4f6bd3c1f9400d45231b16a059531a16f6c", "ground_truth": ["packages/compiler-vapor/src/generators/block.ts"], "issue": 15200, "issue_body": "### Vue version\n\n3.6.0-rc.2\n\n### Link to minimal reproduction\n\n[playground](https://play.vuejs.org/#)\n\n### Steps to reproduce\n\nClick the checkbox/radio, observe the rendered model value.\n\n### What is expected?\n\nClicking the checkbox should update `model` from `false` to `true`.\n\n### What is actually happening?\n\nThe checkbox's DOM checked state changes, but `model` remains `false`.\n\n### System Info\n\n```shell\nSystem:\n  OS: macOS 15.4.1\n  CPU: (14) arm64 Apple M4 Pro\n  Memory: 1016.27 MB / 48.00 GB\n  Shell: 5.9 - /bin/zsh\nBinaries:\n  Node: 26.1.0 - /usr/local/bin/node\n  Yarn: 1.22.22 - /usr/local/bin/yarn\n  npm: 11.13.0 - /usr/local/bin/npm\n  pnpm: 11.13.0 - /usr/local/bin/pnpm\nBrowsers:\n  Chrome: 150.0.7871.187\n  Firefox: 151.0.2\n  Safari: 18.4\n```\n\n### Any additional comments?\n\n_No response_", "issue_title": "[Vapor] v-model does not update for an input with an initially dynamic checkbox/radio type", "pr": 15201, "pr_title": "fix(compiler-vapor): generate v-model after prop effects", "repo": "vuejs/core"}
{"base_sha": "4616c021f60db9c8ec9f72a59cb9804458cf6ab7", "fix_sha": "c9389cc65e44b6cd60e56e2a7db1c4d2721501d5", "ground_truth": ["packages/compiler-sfc/src/script/resolveType.ts"], "issue": 15174, "issue_body": "### Vue version\n\n```\n3.6.0-rc.2\n```\n\n### Link to minimal reproduction\n\nhttps://play.vuejs.org/#\n\n### Steps to reproduce\n\nThe repro has four files. `App.vue` reads a recursive type through an indexed access type\n(`TreeNode['parent']`), and `Second.vue` / `Third.vue` are identical components whose props\ntype resolves to that same recursive type.\n\n**After opening the link, click two tabs in the file bar:**\n\n1. Click the **`Second.vue`** tab. It compiles fine.\n2. Click the **`Third.vue`** tab. `compileScript` throws.\n\nThe clicks are only needed because the Playground compiles files lazily, one per active tab.\nWhat matters is the sequence of three `compileScript` calls in a single process:\n\n1. the component using `TreeNode['parent']` (the indexed access)\n2. a component whose props type resolves to `TreeNode`\n3. a second component whose props type resolves to `TreeNode` \u2014 this one throws\n\nThe files:\n\n```ts\n// types.ts\nexport type TreeNode = {\n  parent?: TreeNode;\n};\n\nexport type ItemProps = {\n  node?: TreeNode;\n};\n```\n\n```vue\n<!-- App.vue -->\n<script setup lang=\"ts\">\nimport { type TreeNode } from './types.ts';\nimport Second from './Second.vue';\nimport Third from './Third.vue';\n\ndefineProps<{ parentNode?: TreeNode['parent'] }>();\n</script>\n\n<template>\n  <Second />\n  <Third />\n</template>\n```\n\n```vue\n<!-- Second.vue and Third.vue, identical -->\n<script setup lang=\"ts\">\nimport { type ItemProps } from './types.ts';\n\ndefineProps<ItemProps>();\n</script>\n\n<template>\n  <div />\n</template>\n```\n\nReproduces the same way outside the Playground, in plain Node against\n`@vue/compiler-sfc@3.6.0-rc.2`, by calling `compileScript` on the three files in that order.\nIt also reproduces with the `esm-browser` build, with and without `registerTS`, and with both\na real filesystem and an in-memory `fs` option.\n\n### What is expected?\n\nAll three components compile, as they do on 3.5.40.\n\nSame files, same click sequence, Vue 3.5.40 (control):\nhttps://play.vuejs.org/#\n\n### What is actually happening?\n\n`compileScript` throws:\n\n```\nRangeError: Maximum call stack size exceeded\n    at SyncWalker.visit (@vue/compiler-sfc@3.6.0-rc.2/dist/compiler-sfc.esm-browser.js)\n    at SyncWalker.visit (\u2026)\n    at SyncWalker.visit (\u2026)\n    \u2026 ~5400 frames\n```\n\n**Cause.** `resolveTypeReference` caches the resolved declaration on the reference node itself:\n\n```js\nreturn canCache ? (node._resolvedReference = resolved) : resolved\n```\n\n`_resolvedReference` holds an AST node, so it has a `.type` string. `compileScript` then walks\neach top-level `<script setup>` statement with `estree-walker`, whose `visit` iterates **every\nenumerable own key** and recurses into any value with a `.type` string. It therefore follows\n`_resolvedReference` as if it were an AST child. Once a recursive type has had its recursive\nmember resolved, the reference graph contains a back-edge, and the walk never terminates.\n\nInstrumenting the walker on the real-world case gives the closed loop (the last node is already\nan ancestor):\n\n```\n(root):VariableDeclaration\n  -> declarations[0]:VariableDeclarator \"props\"\n  -> init:CallExpression                          // defineProps<Props>()\n  -> typeParameters:TSTypeParameterInstantiation\n  -> params[0]:TSTypeReference \"Props\"\n  -> _resolvedReference:TSIntersectionType\n  -> types[0]:TSTypeReference \"Omit\"\n  -> typeParameters:TSTypeParameterInstantiation\n  -> params[0]:TSTypeReference \"BudgetCreateStepsControlsProps\"\n  -> _resolvedReference:TSTypeLiteral {11 members}\n  -> members[1]:TSPropertySignature \"budget\"\n  -> typeAnnotation:TSTypeAnnotation\n  -> typeAnnotation:TSTypeReference \"Budget\"\n  -> _resolvedReference:TSTypeLiteral {63 members}\n  -> members[45]:TSPropertySignature \"parent\"     // parent?: Budget & { \u2026 }\n  -> typeAnnotation:TSTypeAnnotation\n  -> typeAnnotation:TSIntersectionType\n  -> types[0]:TSTypeReference \"Budget\"            // back to the ancestor above\n```\n\nThe `_resolvedReference` caching line is identical in 3.5.40 and 3.6.0-rc.2, so the caching is\nnot itself new. What changed is that on 3.6 the back-edge is reachable from the statement AST\nthat `compileScript` walks. On 3.5.40 the same file set produces no reachable back-edge.\n\n**Verification.** Skipping that single key in the walk clears it. Patching the walker to ignore\n`_resolvedReference` let all 3896 SFCs in our application compile on 3.6.0-rc.2, where the\nunpatched compiler crashes. Ignoring every `_`-prefixed key works too.\n\nA possible fix is to make these internal caches non-enumerable, for example\n`Object.defineProperty(node, '_resolvedReference', { value: resolved, writable: true, configurable: true, enumerable: false })`,\nso `for\u2026in`-based walkers cannot mistake them for AST children. The same applies to any other\ninternal annotation attached to AST nodes that holds a node.\n\n### System Info\n\n```shell\n  System:\n    OS: macOS 26.6\n    CPU: (10) arm64 Apple M4\n    Memory: 887.94 MB / 16.00 GB\n    Shell: 5.9 - /bin/zsh\n  Binaries:\n    Node: 24.18.0 - /opt/homebrew/opt/node@24/bin/node\n    npm: 11.16.0 - /opt/homebrew/opt/node@24/bin/npm\n    pnpm: 11.15.1 - /opt/homebrew/opt/node@24/bin/pnpm\n  Browsers:\n    Chrome: 150.0.7871.187\n    Firefox: 153.0.1\n    Safari: 26.6\n  npmPackages:\n    vue: 3.6.0-rc.2 => 3.6.0-rc.2\n```\n\n### Any additional comments?\n\nFound while upgrading a Vue 3 application (about 3900 SFCs, Vite 8.1.5,\n`@vitejs/plugin-vue` 6.0.8) from 3.5.40 to 3.6.0-rc.2. The only change was the `vue` version.\n\nIn a real project the failure looks non-deterministic, which made it confusing to diagnose:\n\n- The production build failed on one component, the dev server on a different one. Which file\n  crashes depends on the order in which the shared type-resolution cache gets warmed, not on\n  anything about the file itself.\n- Every crashing component compiles fine on its own. It only fails once other components have\n  been compiled in the same process.\n- In dev, the resulting Vite error overlay then blocks unrelated interactions, so the visible\n  symptom appears in features that have nothing to do with the types involved.\n\n", "issue_title": "compiler-sfc: recursive type + indexed access makes compileScript walk a cyclic AST (RangeError: Maximum call stack size exceeded)", "pr": 15175, "pr_title": "fix(compiler-sfc): avoid walking cached type references", "repo": "vuejs/core"}
{"base_sha": "bf5978815d541e976d0705b7f4864e0ad518fa70", "fix_sha": "8a8d972fa734a741df5c42619e4e677ae6a2cacc", "ground_truth": ["packages/runtime-vapor/src/directives/custom.ts"], "issue": 15157, "issue_body": "### Vue version\n\nVue 3.6.0 RC.2 (Also checked the latest, the HEAD commit of minor branch)\n\n### Link to minimal reproduction\n\nhttps://github.com/liang-demos/vue-vapor-custom-directive-interop-repro.git\n\n### Steps to reproduce\n\nThis repo includes two broken cases \n1. `Vapor -> vDOM` interop\n2. `<component :is=\"....\" v-custom>`\n\nFollow the recipe in the repo's README.\n\n### What is expected?\n\nThe directive should work without any error throwing.\n\nThis is because the current `withVaporDirectives` only receives `Element | VaporComponentInstance` as node, and only call `getRootElement` once. So any dynamic component such as `<component>` breaks it.\n\nAs a result, a Vapor component with `v-if`-root template will also have behavior mismatch with vDOM one. Directives won't be applied after v-if condition changes but vDOM's do.\n\n### What is actually happening?\n\nVue runtime throws an error for both two cases.\n\n```log\n[vapor-vdom] TypeError: element.setAttribute is not a function\n    at customDirective.ts:30:13\n    at withVaporDirectives (vue.js?v=4a6bcd24:16898:15)\n    at _sfc_render (VaporToVdom.vue:5:53)\n    at callWithErrorHandling (vue.js?v=4a6bcd24:2432:17)\n    at callRender (vue.js?v=4a6bcd24:12891:9)\n    at devRender (vue.js?v=4a6bcd24:12905:44)\n    at handleSetupResult (vue.js?v=4a6bcd24:13202:3)\n    at setupComponent (vue.js?v=4a6bcd24:12868:9)\n    at createComponent (vue.js?v=4a6bcd24:12825:9)\n    at mountApp (vue.js?v=4a6bcd24:13274:39)\n```\n\n### System Info\n\n```shell\nNeed to install the following packages:\nenvinfo@7.21.0\nOk to proceed? (y) y\n\n  System:\n    OS: macOS 26.5.2\n    CPU: (15) arm64 Apple M5 Pro\n    Memory: 3.44 GB / 48.00 GB\n    Shell: 5.9 - /bin/zsh\n  Binaries:\n    Node: 24.15.0 - /Users/liangmi/.vite-plus/js_runtime/node/24.15.0/bin/node\n    npm: 11.12.1 - /Users/liangmi/.vite-plus/js_runtime/node/24.15.0/bin/npm\n    pnpm: 11.13.0 - /Users/liangmi/.vite-plus/bin/pnpm\n  Browsers:\n    Safari: 26.5.2\n  npmPackages:\n    vue: 3.6.0-rc.2 => 3.6.0-rc.2\n```\n\n### Any additional comments?\n\nI'm actively fixing this.\n\nI think a proper way to fix this is to change `withVaporDirectives` fn, allow it to call `getRootElement` after component updates instead of calling once.", "issue_title": "Vapor: custom directives are broken for VaporFragment", "pr": 15158, "pr_title": "fix(runtime-vapor): handle custom directives on fragment roots", "repo": "vuejs/core"}
{"base_sha": "a7513a1a3ed411ccd90bf135c6cba9c9cb2ef10b", "fix_sha": "71f9ff58a2a8748b2c1ec26c55c89f7343b86110", "ground_truth": ["packages/runtime-dom/src/apiCustomElement.ts"], "issue": 15153, "issue_body": "### Vue version\n\n3.5.40 (`latest` at the time of writing \u2014 the inline repro pins it; also reproduced on 3.5.13, 3.5.17, 3.5.30 and 3.6.0-rc.2; 3.4.38 \u2014 before the nested-async chaining existed \u2014 crashes with the same null dereference in the older `_setParent` provides copy inside `vnode.ce`)\n\n### Link to minimal reproduction\n\nhttps://play.vuejs.org/#\n\n### Steps to reproduce\n\n1. Open the reproduction link and watch the console: `mounted x-inner` never appears \u2014 the element crashes with an unhandled `TypeError: Cannot read properties of null (reading 'provides')` and never mounts. (The playground variant inserts the nested markup dynamically after the `define()` calls; dynamic and parser-created markup crash identically.)\n2. Parser-created (static server-HTML-style) variant \u2014 self-contained single file, no build step (save as `repro.html`, serve statically, open in any browser; Chromium, Firefox and WebKit all reproduce):\n\n```html\n<!doctype html>\n<meta charset=\"utf-8\" />\n<title>nested async custom elements repro</title>\n<x-outer>\n  <x-middle>\n    <div>\n      <x-inner>inner light dom</x-inner>\n    </div>\n  </x-middle>\n</x-outer>\n<script type=\"module\">\n  import {\n    defineCustomElement,\n    defineAsyncComponent,\n    h,\n  } from \"https://unpkg.com/vue@3.5.40/dist/vue.esm-browser.js\";\n\n  // Delays chosen so the middle level settles AFTER its child: outer fast,\n  // middle slow, inner fast. This crashes on 100% of loads.\n  const delays = { \"x-outer\": 50, \"x-middle\": 300, \"x-inner\": 10 };\n\n  const makeComponent = (name) => ({\n    name,\n    mounted() {\n      console.log(`mounted ${name}`);\n    },\n    render() {\n      return h(\"div\", { style: \"display:block;padding:6px;border:1px solid #888\" }, [name, h(\"slot\")]);\n    },\n  });\n\n  const makeLoader = (tag) => () =>\n    new Promise((resolve) => setTimeout(() => resolve(makeComponent(tag)), delays[tag]));\n\n  // Define the three tags parent-first.\n  for (const tag of [\"x-outer\", \"x-middle\", \"x-inner\"]) {\n    customElements.define(tag, defineCustomElement(defineAsyncComponent(makeLoader(tag))));\n  }\n\n  window.addEventListener(\"error\", (e) => console.error(\"UNCAUGHT:\", e.message));\n  window.addEventListener(\"unhandledrejection\", (e) =>\n    console.error(\"UNHANDLED REJECTION:\", e.reason?.message ?? e.reason),\n  );\n</script>\n```\n\n### What is expected?\n\nAll three elements mount ancestor-first: `mounted x-outer`, `mounted x-middle`, `mounted x-inner`. The nested-async chaining added in 3.5.0 (37ccb9b, \"delay mounting of custom elements with async parent\", closing #9341 / #8127 / #9351) exists to guarantee exactly this.\n\n### What is actually happening?\n\n```\nmounted x-outer\nUNHANDLED REJECTION: Cannot read properties of null (reading 'provides')\nmounted x-middle\n```\n\n`x-inner` crashes in `VueElement._mount \u2192 _inheritParentContext` (`runtime-dom` \u2014 `parent._instance.provides` is dereferenced guarded only by `parent && this._app`), never mounts, and is left permanently half-resolved: `_resolved: true`, `_instance: null`, shadow root empty. Nothing ever retries it, no error surfaces through `app.config.errorHandler`, and any `provide`/`inject` inheritance it should have received is lost. Its own descendants (at deeper nesting) then crash against it in cascade.\n\n### Any additional comments?\n\n**Analysis** (why depth \u2265 3 is the trigger)\n\n`connectedCallback` of a child under a *pending* async parent chains:\n\n```js\nthis._pendingResolve = parent._pendingResolve.then(() => {\n  this._pendingResolve = undefined;\n  this._resolveDef();\n});\n```\n\n- For an **unchained** parent (depth 2), `parent._pendingResolve` is the parent's own `_resolveDef()` flow, which settles after the parent has mounted \u2192 2-level nesting is safe. The variants table below confirms: 2 levels never crash.\n- For a **chained** parent (depth \u2265 3), `parent._pendingResolve` is itself a chain promise \u2014 and that promise settles when the parent's `_resolveDef()` **returns** (i.e. when the parent's async load *starts*), not when the parent mounts.\n\nSo once the first unchained ancestor mounts, the whole chain unwinds within a few microtasks: every deeper level's loader fires nearly simultaneously and mount order degenerates to **network settlement order**. Any child whose async definition settles before its parent has mounted hits `_inheritParentContext` while `parent._instance === null` and throws. With real-network chunk timings the victim set varies per load, so in an app this presents as a flaky, page-dependent breakage; with the fixed delays above it is 100% deterministic.\n\nVariants checked (each re-run in a real browser):\n\n| Variant | Result |\n| --- | --- |\n| 3 levels, middle slowest | crashes every load |\n| 3 levels, inner slowest | clean (race direction confirmed) |\n| 2 levels | clean (chained parent required) |\n| plain `<div>` between levels | still crashes (structure-independent) |\n| markup inserted dynamically after all `define()` calls | still crashes |\n| all loaders instant | clean (microtask FIFO protects when nothing is pending) |\n\n`_inheritParentContext` (#13219, ~3.5.14) did not introduce the race \u2014 it converted a pre-existing silent misordering into a hard crash. 3.4.38 (no chaining) and 3.5.13 (chaining, pre-#13219) fail with the same null dereference in the older `_setParent` provides-copy path.\n\n**Suggested direction** \u2014 either (or both):\n\n1. Chain the child to the parent's actual **mount**, not to `_resolveDef()` returning \u2014 e.g. resolve the parent's `_pendingResolve` only after `_mount()` has run, so the guarantee the chaining was built for (\"ancestor mounts before descendant\") holds at any depth.\n2. Guard `_inheritParentContext` against `parent._instance === null` and defer context inheritance until the parent instance exists, instead of throwing away the child permanently on an unhandled rejection.\n", "issue_title": "Custom elements: async components nested \u22653 levels crash with `TypeError: Cannot read properties of null (reading 'provides')` \u2014 chained child resolve races its parent's mount", "pr": 15154, "pr_title": "fix(custom-element): preserve nested async mount order", "repo": "vuejs/core"}
{"base_sha": "71f9ff58a2a8748b2c1ec26c55c89f7343b86110", "fix_sha": "c04a45d15498ebba0de3ceae674b88ac43cb9e76", "ground_truth": ["packages/runtime-core/src/components/Suspense.ts", "packages/runtime-core/src/scheduler.ts"], "issue": 15142, "issue_body": "### Vue version\n\n3.5.39 (also reproduces on 3.5.40 / `@b5f8518` in the playground)\n\n### Link to minimal reproduction\n\nhttps://play.vuejs.org/#\n\n### Steps to reproduce\n\n1. Open the playground and show the **Preview** pane.\n2. The app renders a `<Suspense>` whose default slot contains an async component (resolves after ~4s) plus a large tree (2000 rows, each with a few `flush: 'post'` watchers).\n3. While the suspense is pending, a timer keeps mutating a ref, so the still-mounted tree re-renders repeatedly. Each re-render buffers its post effects into `suspense.effects` (via `queueEffectWithSuspense`).\n4. When the async component resolves, the suspense flushes the buffered effects.\n\n### What is expected?\n\nThe buffered effects flush and the app keeps working.\n\n### What is actually happening?\n\n`Uncaught (in promise): RangeError: Maximum call stack size exceeded` \u2014 thrown from `queuePostFlushCb` called by `suspense.resolve()`:\n\n```text\nRangeError: Maximum call stack size exceeded\n    at queuePostFlushCb (runtime-core.esm-bundler.js:331)\n    at Object.resolve   (runtime-core.esm-bundler.js:7426)\n```\n\nThe scheduler dies mid-flush, leaving the app permanently wedged (blank page).\n\n### Root cause\n\n`suspense.effects` is unbounded \u2014 every post effect of every component under a pending suspense boundary is buffered into it. On resolve, the whole array is flushed with a spread call:\n\n- `scheduler.ts`: `pendingPostFlushCbs.push(...cb)`\n- `scheduler.ts` (`flushPostFlushCbs` nested case): `activePostFlushCbs.push(...deduped)`\n- `Suspense.ts` (merge into pending parent): `parent.effects.push(...effects)`\n\nSpreading an array into a function call is limited by the engine's call stack (~100k\u2013200k args in V8), so once enough effects accumulate the flush itself throws.\n\n### Real-world impact\n\nWe hit this in production in a Nuxt app: a kanban board page with ~1000 mounted cards that re-renders once per second (elapsed-time ticker). Navigating to another page keeps the board mounted under `<NuxtPage>`'s pending Suspense; its buffered effects exceed the spread limit and every SPA navigation away from the board crashes with a permanent white screen (hard reload of the target URL works fine, since there is no pending suspense in that case).\n\n### Suggested fix\n\nReplace the unbounded spread pushes with plain loops. I have a PR ready with the fix and a regression test (queuePostFlushCb with a 1M-entry array).", "issue_title": "Suspense: flushing buffered effects overflows the call stack when many effects accumulate while pending (RangeError: Maximum call stack size exceeded)", "pr": 15143, "pr_title": "fix(scheduler): avoid stack overflow when flushing very large post cb arrays", "repo": "vuejs/core"}
{"base_sha": "a686662492f00c4cb2552cf9ffcc39fc13e0730f", "fix_sha": "e9cb7c86109f860c873470751a7cc653f9f31f43", "ground_truth": ["packages/compiler-vapor/src/generate.ts", "packages/compiler-vapor/src/generators/expression.ts"], "issue": 15118, "issue_body": "\n### Vue version\n3.6.0-rc.1\n\n### Steps to reproduce\n\nAny vapor SFC with a `v-for` whose **loop variable is named `child`**, where the body both (a) reads a static child node via the `child` helper and (b) has multiple reactive reads of the loop item (so the codegen caches it):\n\n```vue\n<script setup lang=\"ts\" vapor>\nconst items = [\n  { key: 'a', label: 'Alpha', disabled: false },\n  { key: 'b', label: 'Beta', disabled: true },\n]\nfunction pick(key: string) {}\n</script>\n\n<template>\n  <div>\n    <button\n      v-for=\"child in items\"\n      :key=\"child.key\"\n      :disabled=\"child.disabled\"\n      @click=\"pick(child.key)\"\n    >\n      {{ child.label }}\n    </button>\n  </div>\n</template>\n```\n\nMount it (directly or via `vaporInteropPlugin`) \u2192 runtime throws.\n\n### What is expected?\n\nRenders the list. The codegen cache variable must not collide with imported runtime helpers.\n\n### What is actually happening?\n\n```\nReferenceError: Cannot access '_child' before initialization\n    at ... (Component.vue:707)\n    at mount (runtime: createFor \u2192 renderList \u2192 mount)\n```\n\nActual compiled output (from our app, vite dev):\n\n```js\n(_for_item1) => {\n  const n29 = t2();\n  const n28 = _child(n29);          // \u2190 call to the *imported* helper `_child`\n  const x28 = _txt(n28);\n  n29.$evtclick = _createInvoker(() => _ctx.handleSelect(_for_item1.value.key));\n  let _child;                        // \u2190 codegen names its item-cache `_child` (from `v-for=\"child in \u2026\"`)\n  _renderEffect(() => {\n    _child = _for_item1.value;\n    _setProp(n29, \"disabled\", _child.disabled);\n    _setText(x28, _toDisplayString(_child.label));\n  });\n  _selector26(_child.key, () => { ... });\n  return n29;\n},\n```\n\nThe local `let _child` (derived from the user's loop variable name `child` with the `_` prefix) shadows the imported `child` helper (aliased `_child`) within the same function scope, so the earlier `_child(n29)` call hits the temporal dead zone.\n\nRenaming the loop variable (e.g. `v-for=\"sub in items\"`) makes the component work \u2014 which also means any codebase using the very common name `child` in a v-for crashes under Vapor.\n\nLikely fix: codegen should reserve/uniquify cache identifiers against the helper alias set (same class of problem as #14296, but for v-for item cache vs runtime helper imports).\n\nFound while migrating a ~220-SFC production app to vapor mode; happy to provide more compiled-output samples if useful.\n", "issue_title": "[Vapor] compiler: v-for item named `child` generates cache variable `_child` that shadows the imported `child` runtime helper \u2192 TDZ ReferenceError", "pr": 15124, "pr_title": "fix(compiler-vapor): avoid helper and cache variable name collisions", "repo": "vuejs/core"}
{"base_sha": "0516c43284084f0526032a1cc312fb6916bf04fe", "fix_sha": "b6191cbf95c680e076164a319dd987a14de658c1", "ground_truth": ["packages/runtime-core/src/component.ts"], "issue": 15113, "issue_body": "### Vue version\n\n3.5.40\n\n### Steps to reproduce\n\noriginally reported in https://github.com/nuxt/nuxt/issues/30782\n\n1. create an options API component with an `async setup()`, a `computed` property that reads shared module-level state, and a `watch` on that\n2. render it on the server and then mutate the shared state after the render is done\n3. see that the watcher is still active on the server\n\n**Reproduction**\n```ts\ntest('options-api watchers should not be created for async setup components', async () => {\n  const open = ref(true)\n  const spy = vi.fn()\n\n  const App = defineComponent({\n    setup: () => Promise.resolve({}),\n    computed: {\n      isOpen() {\n        return open.value\n      },\n    },\n    watch: {\n      isOpen: spy,\n    },\n    render() {\n      return h('div', String(this.isOpen))\n    },\n  })\n\n  const html = await renderToString(createSSRApp(App))\n  expect(html).toBe('<div>true</div>')\n\n  // mutating the state after the request is done should not trigger the watcher\n  open.value = false\n  await nextTick()\n  expect(spy).not.toHaveBeenCalled()\n})\n```\n\n### What is expected?\n\nthe same behavior as with a synchronous setup: regular watchers declared via the `watch` option don't run during SSR and don't survive the render\n\nmutating the shared state after `renderToString` resolves should not trigger anything\n\n### What is actually happening?\n\nwhen `setup()` returns a `Promise`, the `watch` option creates a live watcher on the server that is never stopped\n\nif this subscribes to state that **outlives the request**, it keeps the **entire component instance (and app) reachable forever**\n\n### System Info\n\n```shell\n\n```\n\n### Any additional comments?\n\n_No response_", "issue_title": "SSR memory leak - options API watchers are created and never stopped with async `setup()`", "pr": 15114, "pr_title": "fix(runtime-core): restore SSR setup state when handling async setup result", "repo": "vuejs/core"}
{"base_sha": "37bf752bf1ff3df023f38fe43a8f12564db23b85", "fix_sha": "0516c43284084f0526032a1cc312fb6916bf04fe", "ground_truth": ["packages/runtime-core/src/apiSetupHelpers.ts"], "issue": 15096, "issue_body": "### Vue version\n\n3.5.39\n\n### Link to minimal reproduction\n\nhttps://play.vuejs.org/#\n\n### Steps to reproduce\n\nIn the reproduction, `broken` declares a named model with a runtime `type: PropType<string[]>` plus a factory `default: () => []`. `fixed` declares the same thing but with the generic pinned explicitly (`defineModel<string[]>(...)`).\n\nRun the type checker (or `vue-tsc --noEmit` locally) over this SFC.\n\n### What is expected?\n\nBoth `broken` and `fixed` should type-check cleanly \u2014 this is the recommended pattern for avoiding shared mutable state (per #14966 / #14968): a factory default combined with a runtime `PropType` declaration.\n\n\n### What is actually happening?\n\nOnly `broken` fails. TS infers the generic `T` from the `default` factory function's own type (`() => never[]`) instead of from the `type: PropType<string[]>` field:\n\nerror TS2769: No overload matches this call.\nOverload 1 of 4, '(name: string, options: ({ required: true; } | { default: DefineModelDefault<() => never[]>; }) & Omit<PropOptions<() => never[], () => never[]>, \"default\"> & DefineModelOptions<...>): ModelRef<...>', gave the following error.\nType 'PropType<string[]>' is not assignable to type 'true | PropType<() => never[]> | null | undefined'.\nType 'new (...args: any[]) => string[]' is not assignable to type 'true | PropType<() => never[]> | null | undefined'.\nType 'new (...args: any[]) => string[]' is not assignable to type 'new (...args: any[]) => () => never[]'.\nType 'string[]' is not assignable to type '() => never[]'.\nType 'string[]' provides no match for the signature '(): never[]'.\nOverload 2 of 4, '(name: string, options?: DefineModelRuntimeOptions<string[], string[], string[]> | undefined): ModelRef<string[] | undefined, string, string[] | undefined, string[] | undefined>', gave the following error.\nObject literal may only specify known properties, and 'default' does not exist in type 'DefineModelRuntimeOptions<string[], string[], string[]>'.\n\nhis is a regression from #14968 (landed in 3.5.39), which introduced `DefineModelDefault<T> = InferDefault<Data, T>` for the named-model overload. When both a runtime `type: PropType<T>` field and a factory `default` are present on the same options object, TS resolves `T` from the `default` field first, breaking a pattern that worked prior to 3.5.39.\n\n**Workaround:** explicitly pin the generic, as shown by `fixed` in the reproduction:\n\n```ts\nconst fixed = defineModel<string[]>('visibility2', {\n  type: Array as PropType<string[]>,\n  default: () => [],\n})\n```\n\n### System Info\n\n```shell\nvue: 3.5.39\nvue-tsc: 3.3.7\ntypescript: 5.7.3\n```\n\n### Any additional comments?\n\nRelated to / regression from #14966 and #14968. Only reproduces on the **named** `defineModel(name, options)` overload when `type`/`PropType` and a factory `default` are combined; the unnamed `defineModel<T>({ ... })` form with an explicit generic is unaffected.", "issue_title": "defineModel: named model with PropType + factory default infers wrong generic (regression from #14968)", "pr": 15097, "pr_title": "fix(types): preserve defineModel inference with factory defaults", "repo": "vuejs/core"}
{"base_sha": "fe1c0f8494c10bc677b0bf8ec7fa518e3f15569e", "fix_sha": "97f3525551ad06103083eb2cbf9858c5e57cd58e", "ground_truth": ["packages/runtime-core/src/apiAsyncComponent.ts"], "issue": 15091, "issue_body": "### Vue version\n\n3.5.33 \n\n### Link to minimal reproduction\n\nhttps://stackblitz.com/edit/vitejs-vite-ktsycrrc?file=repro.mjs\n\n### Steps to reproduce\n\n1. SSR-render an async component that uses hydrate: hydrateOnVisible().\n2. On the client, hydration is deferred and the strategy registers an IntersectionObserver on the component's SSR node.\n3. The node gets detached from the document before the observer's callback executes (in production this is a route change / re-render while the intersection callback is already queued).\n4. The observer callback runs and invokes hydrate() against the now-detached node.\n\n### What is expected?\n\nA node that is no longer connected to the document should simply not be hydrated (there's nothing to hydrate), no exception thrown.\n\n### What is actually happening?\n\nhydrate() reaches hydrateNode, which computes the container type from the node's parent:\n\n```\n// packages/runtime-core/src/hydration.ts \u2014 inside hydrateNode, async-component branch\nnextNode = vnode.type.hydrate(\n  node, vnode, parentComponent, parentSuspense,\n  getContainerType(parentNode(node)),   // parentNode(node) === null for a detached node\n  slotScopeIds, optimized, rendererInternals, hydrateNode,\n)\n```\n\n```\n// packages/runtime-core/src/hydration.ts\nconst getContainerType = (container) => {\n  if (container.nodeType !== 1) return undefined   // <-- container is null -> throws\n  ...\n}\n```\n\nFor a detached node parentNode(node) is null, so getContainerType(null) throws TypeError: Cannot read properties of null (reading 'nodeType').\n\nThe async-component hydration wrapper only guards against the patched case, never against disconnection:\n\n```\n// packages/runtime-core/src/apiAsyncComponent.ts \u2014 __asyncHydrate\nconst performHydrate = () => {\n  if (patched) { /* warn + */ return }   // only guards `patched`\n  hydrate()                              // no isConnected check\n}\n```\nBecause the strategy runs inside load().then(...), the throw surfaces as an unhandledrejection, so app-level try/catch around the component can't catch it.\n\n### System Info\n\n```shell\nvue: 3.5.33\n@vue/server-renderer: 3.5.33\nEnvironment where it triggers: Android WebView (Chrome), production SSR + client hydration\n```\n\n### Any additional comments?\n\nWe see this almost exclusively on Android Chrome WebView (~43k occurrences / ~12.8k users over a rollout window) on a page that lazy-hydrates several below-the-fold sections via hydrateOnVisible. The IntersectionObserver callback is dispatched asynchronously; when the user navigates/re-renders in the WebView, the observed node is detached between the intersection being recorded and the callback running, exactly the sequence above. Desktop rarely hits the race.\n\nSkip hydration when the anchor node is no longer connected. the semantically correct place is the async performHydrate (or each built-in strategy's trigger), e.g. bail when no observed/anchor element isConnected:\n \n```\nconst performHydrate = () => {\n  if (patched) { return }\n  if (el && !el.isConnected) return   // detached before hydration \u2192 nothing to hydrate\n  hydrate()\n}\n```\nAs defense-in-depth, getContainerType could also tolerate a `null` container `(if (!container || container.nodeType !== 1) return undefined)`, since it's called with parentNode(node) at several sites.\n\nWe're currently shipping a userland wrapper around hydrateOnVisible that applies exactly the isConnected guard, which fully stops the crash.", "issue_title": "hydrateOnVisible: Cannot read properties of null (reading 'nodeType') when the SSR node is detached before the IntersectionObserver callback fires", "pr": 15092, "pr_title": "fix(runtime-core): skip lazy hydration for detached roots", "repo": "vuejs/core"}
{"base_sha": "3d4ca4bbd142e1b9a967cd599e55cb143eab11a3", "fix_sha": "06881f71f0cd9f125b5319069ef090eba2b1f40a", "ground_truth": ["src/locale/de/_lib/match/index.ts"], "issue": 4148, "issue_body": "It is not possible to parse the string \"6. J\u00e4nner\" with the locale set to deAT.\n\nSource encoding is UTF-8\nNode.js version 22.20.0\n\nSource:\n```javascript\nimport { de, deAT } from \"date-fns/locale\";\nimport { parse as dateFnsParse } from \"date-fns/parse\";\n\nvar options = {};\n\nconsole.log(\"16. J\u00e4nner\");\noptions.locale = deAT;\nvar result = dateFnsParse(\"16. J\u00e4nner\", \"dd. MMMM\", new Date(), options);\nconsole.log(result.toDateString());\nconsole.log(\"\\n\\n\");\n\nconsole.log(\"16. J\\xE4nner\");\noptions.locale = deAT;\nvar result = dateFnsParse(\"16. J\\xE4nner\", \"dd. MMMM\", new Date(), options);\nconsole.log(result.toDateString());\nconsole.log(\"\\n\\n\");\n\nconsole.log(\"16. Januar\");\noptions.locale = de;\nvar result = dateFnsParse(\"16. Januar\", \"dd. MMMM\", new Date(), options);\nconsole.log(result.toDateString());\nconsole.log(\"\\n\\n\");\n```\n\nProduces:\n```\n\n> nodejs-test@0.1.0 run\n> node index.js\n\n16. J\u00e4nner\nInvalid Date\n\n\n\n16. J\u00e4nner\nInvalid Date\n\n\n\n16. Januar\nFri Jan 16 2026\n\n\n\n```", "issue_title": "Cannot parse \"J\u00e4nner\" with deAT locale", "pr": 4179, "pr_title": "fix(de-AT): support parsing \"J\u00e4nner\" with de-AT locale", "repo": "date-fns/date-fns"}
