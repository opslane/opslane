import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { OpslaneClient } from './client.js';

export function registerTools(_server: McpServer, _client: OpslaneClient): void {
  // Tools land in Task 4.
}
