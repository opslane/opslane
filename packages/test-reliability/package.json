{
  "name": "@opslane/test-reliability",
  "version": "0.0.1",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "tsc -p tsconfig.typecheck.json && vitest run",
    "test:system": "tsc -p tsconfig.typecheck.json && vitest run --config vitest.system.config.ts"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.3",
    "typescript": "^5.4.0",
    "vitest": "^3.2.0"
  }
}
